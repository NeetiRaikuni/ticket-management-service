package com.management.booking.readonlyrepository;


// for future cases where we would want to have separate read replicas and this would be used for only reading. all the read only apis will be moved here.

public interface BookingReadOnlyRepository {
}
